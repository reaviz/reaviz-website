import { PropsTable } from '@/components/ui/props-table';
import { Canvas, Meta } from '@storybook/blocks';
import { ToggleCanvas } from '@/components/ui/toggle-canvas';
import { Runner } from '@/components/ui/runner';
import * as VennDiagramStory from '../../../stories/components/VennDiagram.story.tsx';

<Meta of={VennDiagramStory} />

# Venn Diagram

A Venn diagram or set diagram is a diagram that shows all possible logical relations
between a finite collection of sets.

The chart supports 3 layout types:
- [Venn](/docs/charts/venn-diagram#venn)
- [Euler](/docs/charts/venn-diagram#euler)
- [Star Euler](/docs/charts/venn-diagram#star-euler)

## Quick Start
To create a venn diagram, import the `VennDiagram` and give it `data`. The chart
will automatically configure itself with the default options exposed via `props`.

<Runner code={`import { VennDiagram } from 'reaviz';

export default function App() {
  return (
    <VennDiagram
      height={300}
      width={300}
      data={[
        { key: ['A'], data: 12 },
        { key: ['B'], data: 12 },
        { key: ['A', 'B'], data: 2 }
      ]}
    />
  );
}`}
/>

One important aspect to realize here is the `key` property
is a array of intersections. It can have one to N number
of relationships.

## Examples
### Venn
<ToggleCanvas of={VennDiagramStory.Simple} storybook="charts-venn-diagram--simple" />

### Euler
<ToggleCanvas of={VennDiagramStory.Euler} storybook="charts-venn-diagram--euler" />

### Star Euler
<ToggleCanvas of={VennDiagramStory.StarEuler} storybook="charts-venn-diagram--star-euler" />

## API
### [VennDiagram](https://github.com/reaviz/reaviz/blob/master/src/VennDiagram/VennDiagram.tsx)
<PropsTable name="VennDiagram" />

### [VennSeries](https://github.com/reaviz/reaviz/blob/master/src/VennDiagram/VennSeries.tsx)
<PropsTable name="VennSeries" />

### [VennArc](https://github.com/reaviz/reaviz/blob/master/src/VennDiagram/VennArc.tsx)
<PropsTable name="VennArc" />

### [VennLabel](https://github.com/reaviz/reaviz/blob/master/src/VennDiagram/VennLabel.tsx)
<PropsTable name="VennLabel" />

### [VennOuterLabel](https://github.com/reaviz/reaviz/blob/master/src/VennDiagram/VennOuterLabel.tsx)
<PropsTable name="VennOuterLabel" />
