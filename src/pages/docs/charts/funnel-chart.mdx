# Funnel Chart

A funnel chart is used to show streamlined data; each slice in the
funnel represents a process that has filtered out data. The last funnel
bears the value that is the final result of the entire procedure.

## Quick Start
To create a funnel chart, use import the `FunnelChart` and give it some `data`.

```jsx
import { FunnelChart } from 'reaviz';

const data = [
  {
    data: 1000,
    key: 'Visited Site'
  },
  {
    data: 900,
    key: 'Added to Cart'
  },
  {
    data: 600,
    key: 'Initiated Checkout'
  },
  {
    data: 400,
    key: 'Completed Purchase'
  }
];

const MyChart = () => (
  <FunnelChart data={data} height={450} width={450} />
);
```

## API

### [FunnelChart](https://github.com/reaviz/reaviz/blob/master/src/FunnelChart/FunnelChart.tsx)

### [FunnelSeries](https://github.com/reaviz/reaviz/blob/master/src/FunnelChart/FunnelSeries/FunnelSeries.tsx)

### [FunnelArc](https://github.com/reaviz/reaviz/blob/master/src/FunnelChart/FunnelSeries/FunnelArc.tsx)

### [FunnelAxis](https://github.com/reaviz/reaviz/blob/master/src/FunnelChart/FunnelSeries/FunnelAxis/FunnelAxis.tsx)

### [FunnelAxisLabel](https://github.com/reaviz/reaviz/blob/master/src/FunnelChart/FunnelSeries/FunnelAxis/FunnelAxisLabel.tsx)

### [FunnelAxisLine](https://github.com/reaviz/reaviz/blob/master/src/FunnelChart/FunnelSeries/FunnelAxis/FunnelAxisLine.tsx)
