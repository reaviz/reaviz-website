import { PropsTable } from '@/components/ui/props-table';
import { Canvas, Meta } from '@storybook/blocks';
import { ToggleCanvas } from '@/components/ui/toggle-canvas';
import * as HeatmapStory from '../../../stories/components/Heatmap.story.tsx';
import * as HeatmapCalendarStory from '../../../stories/components/HeatmapCalendar.story.tsx';
import { CalendarHeatmap } from 'reaviz';

<Meta of={HeatmapStory} />
<Meta of={HeatmapCalendarStory} />

# Heatmap

A heat map is a data visualization type where the individual values
contained in a matrix through variations in coloring. The term “Heat map” was
originally introduced by software designer Cormac Kinney in 1991 to describe a 2D
display depicting real time financial market information even though similar
visualizations have existed for over a century.

Heat maps are useful for visualizing variance across multiple
variables to display patterns in correlations.

Types supported by reaviz:

- [Heatmap](/docs/charts/heatmap#heatmap)
- [Calendar Heatmap](/docs/charts/heatmap#calendar-heatmap)

## Examples

### Heatmap
<ToggleCanvas of={HeatmapStory.Basic} storybook="charts-heatmap-simple--basic" />

### Calendar Heatmap
<ToggleCanvas of={HeatmapCalendarStory.YearCalendar} storybook="charts-heatmap-calendar--year-calendar" />

## Quick Start
To create a Heatmap, use import the `Heatmap` and give it `data`. The chart
will automatically configure itself with the default options exposed via `props`.

```jsx
import { Heatmap } from 'reaviz';

const data = [
  {
    key: 'Lateral Movement',
    data: [
      {
        key: 'XML',
        data: 0
      },
      {
        key: 'JSON',
        data: 120
      },
      {
        key: 'HTTPS',
        data: 150
      }
    ]
  },
  {
    key: 'Discovery',
    data: [
      {
        key: 'XML',
        data: 100
      },
      {
        key: 'JSON',
        data: 34
      },
      {
        key: 'HTTPS',
        data: 0
      }
    ]
  }
];

const MyChart = () => (
  <Heatmap
    height={350}
    width={350}
    data={data}
  />
);
```

## API
### [Heatmap](https://github.com/reaviz/reaviz/blob/master/src/Heatmap/Heatmap.tsx)
<PropsTable name="Heatmap" />

### [CalendarHeatmap](https://github.com/reaviz/reaviz/blob/master/src/Heatmap/CalendarHeatmap.tsx)
<PropsTable name="CalendarHeatmap" />

### [HeatmapSeries](https://github.com/reaviz/reaviz/blob/master/src/Heatmap/HeatmapSeries/HeatmapSeries.tsx)
<PropsTable name="HeatmapSeries" />

### [HeatmapCell](https://github.com/reaviz/reaviz/blob/master/src/Heatmap/HeatmapSeries/HeatmapCell.tsx)
<PropsTable name="HeatmapCell" />
