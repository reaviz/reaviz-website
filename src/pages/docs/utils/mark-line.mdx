import { PropsTable } from '@/components/ui/props-table';

# Markline
Markline is the line shown on the `AreaChart` to indicate a specific value when you hover over the chart.
For example, when you hover in the `AreaChart` area the line will be shown on the 
chart to indicate the value of the hovered point with circles representing the points.

## API

#### [MarkLine](https://github.com/reaviz/reaviz/blob/master/src/common/MarkLine/MarkLine.tsx)
<PropsTable name="MarkLine" />
