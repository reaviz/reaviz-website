import { Meta } from '@storybook/blocks';
import { schemes } from 'reaviz';
import { ColorPaletteBlocks } from 'reablocks';

<Meta title="Utils/Colors" />

# Colors 

Here's a collection of preset color schemes available via the `colorScheme` attribute on most charts. 
We've added all of the [ColorBrewer](https://colorbrewer2.org/) palletes as well.
<br />

{Object.keys(schemes).map(name => (
  <>
    <ColorPaletteBlocks
      name={name}
      className="blocks"
      colors={schemes[name]}
      showNames={false}
    />
    <br />
  </>
))}
