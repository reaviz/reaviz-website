import { PropsTable } from '@/components/ui/props-table';
import { Runner } from '@/components/ui/runner';
import { StoryRenderer } from '@/components/ui/story-renderer';

# Linear Gauge

A Linear Gauge in REAVIZ uses a linear scale to display a specific data point using a dial over a linear scale with defined limits. 
This type of gauge is ideal for showing how a value fits within a range.

### Types supported by reaviz:

- [Single-series gauges](/docs/charts/linear-gauge#single-series-gauges)
- [Multi-series gauges](/docs/charts/linear-gauge#multi-series-gauges)

### Where to use:

- **Display Single Values:** Ideal for showing specific data points within a range.
- **Track Metrics:** Useful for tracking metrics such as risk scores or performance indicators.
- **Compare Multiple Values:** Great for comparing multiple values in a clear, linear format.


## Quick Start
To create a Linear Gauge, use import the `LinearGauge` and give it `data`. The chart
will automatically configure itself with the default options exposed via `props`.

<Runner code={`import { LinearGauge } from 'reaviz';

export default function App() {
  return (
    <LinearGauge
      width={300}
      height={100}
      data={{ key: 'Risk Score', data: 74 }}
    />
  );
}`}
/>

In this example, we only pass one data object, however, we can pass multiples and it will
distribute the values across them.

## Examples

### Single-series gauges
<StoryRenderer path="components/LinearGaugeSingleSeries.story.tsx" name="Simple" storybookKey="charts-gauge-linear-single-series--simple" />

### Multi-series gauges
<StoryRenderer path="components/LinearGaugeMultiSeries.story.tsx" name="_Simple" storybookKey="charts-gauge-linear-multi-series--simple" />

## API
### [LinearGauge](https://github.com/reaviz/reaviz/blob/master/src/LinearGauge/LinearGauge.tsx)
<PropsTable name="LinearGauge" />

### [LinearGaugeSeries](https://github.com/reaviz/reaviz/blob/master/src/LinearGauge/LinearGaugeSeries.tsx)
<PropsTable name="LinearGaugeSeries" />

### [LinearGaugeBar](https://github.com/reaviz/reaviz/blob/master/src/LinearGauge/LinearGaugeBar.tsx)
<PropsTable name="LinearGaugeBar" />

### [LinearGaugeOuterBar](https://github.com/reaviz/reaviz/blob/master/src/LinearGauge/LinearGaugeOuterBar.tsx)
<PropsTable name="LinearGaugeOuterBar" />
